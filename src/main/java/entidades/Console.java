package entidades;

public class Console extends Produto {
    public Console(String codigo, String descricao, String marca, int valorEntrada, int valorSaida, int qtdEstoque, String nome) {
        super(codigo, descricao, marca, valorEntrada, valorSaida, qtdEstoque, nome);
    }
}
